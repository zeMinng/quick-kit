import { JsonWorkbench } from '@/features/json'

const JsonPage: React.FC = () => {
  return (
    <div className="json-page" style={{ height: '100%'}}>
      <JsonWorkbench />
    </div>
  )
}

export default JsonPage